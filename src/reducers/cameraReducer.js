export const initialState = {
    searchTerm = "", 
    photos = [],
    loading: false,
    error: ""
};

export function cameraReducer(state, action) {
  switch (action.type) {
    default:
      return state;
  }
}